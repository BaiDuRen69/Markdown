# .gitignore 使用指南（防御性强化版）

`.gitignore` 是一个“黑名单”文件，告诉 Git 哪些文件或目录**无需跟踪**，避免将敏感信息、编译产物、系统垃圾等提交到仓库。

## 1. 基本语法

- 每行一条规则。
- **行首**的 `#` 表示注释。**行内不能写注释**，`config.json # 配置` 中的 `#` 及后面内容会被当作文件名，千万小心。
- 支持通配符：`*`、`?`、`[]`、`**`。
- `!` 开头表示**取反**，即强制跟踪被前面规则忽略的文件或目录（有严格限制，见下文）。
- 末尾 `/` 仅匹配**目录**；开头 `/` 仅匹配**仓库根目录**。

---

## 2. 常用规则示例（带精确行为说明）

### 忽略特定文件名（任意层级）
```
config.json
secret.key
```

### 忽略特定目录（任意层级，且只匹配目录）
```
logs/
temp/
```
> 哪怕有个普通文件刚好叫 `logs`，也不会被误伤。

### 仅忽略根目录下的文件或目录
```
/logs/
/TODO.txt
```

### 按扩展名忽略（所有层级）
```
*.log
*.tmp
*.swp
```
> `*` 不跨目录，`*.log` 会忽略任意深度的 `.log` 文件。

### 忽略目录下所有内容，但保留特定文件
```
logs/*
!logs/important.log
```
> 前提：`logs` 目录本身没有被忽略（不能上一行有 `logs/`）。如果想保留的是**目录**，必须配合 `**` 递归复活，见进阶部分。

### 递归忽略所有 `node_modules` 目录
```
node_modules/
# 或者更显式地：
**/node_modules/
```
> 加上末尾 `/` 可确保只忽略目录，即便有一天真有个文件叫 `node_modules` 也不会受影响。

### 限定路径的忽略，保留例外
```
/config/*.json
!/config/keep.json
```
> 此规则**只作用于 `/config` 下第一层**，`config/db/setting.json` 不受影响。若需递归忽略 `config` 内所有 `.json`，应写 `config/**/*.json`，再反向排除。

### `?` 匹配单个任意字符
```
?.log
```
> 忽略 `a.log`、`b.log`，但不忽略 `ab.log`。

### `[]` 字符集与范围
```
[abc].log        # 匹配 a.log、b.log、c.log，但不匹配 ab.log
file[0-9].txt    # 仅匹配 file0.txt ~ file9.txt，不匹配 file10.txt
```
> 需要匹配数字超过一位？用 `file[0-9]*.txt` 或分拆规则如 `file[0-9].txt` + `file[0-9][0-9].txt`。

### `**` 匹配任意多级目录
```
**/node_modules/      # 任意层级忽略 node_modules 目录
docs/**/*.pdf         # 忽略 docs 下所有层级的 pdf
```
> 只写 `**` 本身可以匹配整个目录，但一般结合前后路径使用。

---

## 3. 容易被忽略的重要陷阱（救命的）

### 陷阱1：父目录被忽略，子文件无法复活
如果写了：
```
build/
!build/output.txt    # ❌ 无效！build/ 整个目录已忽略，子文件无法单独复活
```
正确做法：
```
build/*
!build/output.txt    # ✅ 先放开目录内容，再捞回特定文件
```

### 陷阱2：行内注释不存在
```
*.log  # 这是注释 —— 错！所有内容都会被当作匹配模式，等于匹配 "*.log  # 这是注释" 这种文件名
```

### 陷阱3：规则对已跟踪文件无效，且会影响其他人
如果一个文件已经 `commit` 过，加到 `.gitignore` 后它**不会自动停止跟踪**。如果之后用 `git rm --cached` 移除跟踪，下一次别人 `pull` 时，**该文件会从他们的工作目录中被删除**。务必提前和团队沟通，或采用平滑迁移方案（如先通知所有人备份）。

### 陷阱4：反转规则 `!` 不递归
```gitignore
ignored/
!ignored/keep/
```
这样写 `keep/` 目录本身会被复活，但里面的文件**不会**。正确写法是：
```
ignored/*
!ignored/keep/
!ignored/keep/**
```
> 原则：想复活一个目录，必须逐层把内容也“捞”回来。

---

## 4. 让新规则安全生效

### 针对新文件（未跟踪）
只要 `.gitignore` 写对，生成新文件时自动被忽略。

### 针对已被跟踪的文件（先想清楚再动手）
需要从 Git 跟踪列表移除，但不删除本地文件：
```bash
git rm --cached 文件名
git commit -m "停止跟踪 文件名"
```

**批量刷新整个仓库——踩坑风险极高，操作前必读**：
```bash
git rm -r --cached .
git add .
git commit -m "更新 .gitignore 规则"
```
⚠️ **必须提前做到**：
1. 确保 `.gitignore` 已经写好并覆盖所有不想跟踪的文件（私密配置、本地环境等）。
2. 用 `git status` 检查，确认没有预期之外的文件被加入暂存区。
3. 如果有未提交的改动，先单独提交或暂存（`git stash`）。
4. **通知所有协作者**，拉取此提交后某些文件会被删除，必要时自行备份。

---

## 5. 检查忽略状态

```bash
# 查看所有被忽略的文件
git status --ignored

# 检查某个文件是否被忽略，并显示是哪条规则生效
git check-ignore -v 文件名
```
> 如果该文件**未被忽略**，`git check-ignore` 不会有任何输出。这不是命令失效，而是表示“该文件正被跟踪或不匹配任何忽略规则”。

---

## 6. 提升：全局忽略与个人本地忽略

### 全局忽略（对本机所有 Git 仓库生效）
适用于 `.DS_Store`、`Thumbs.db`、编辑器临时文件等与项目无关的个人垃圾。
```bash
git config --global core.excludesFile ~/.gitignore_global
```
然后在 `~/.gitignore_global` 中写入规则，如：
```
.DS_Store
*~
*.swp
```

### 仅本地生效的排除（不提交到仓库）
编辑 `.git/info/exclude` 文件，语法与 `.gitignore` 完全相同，仅对当前仓库生效且不会被提交。适合个人 IDE 配置等。

---

## 7. 核对清单（提交前自检）

- [ ] 有没有行末尾无意写了注释？
- [ ] 忽略目录后，有没有试图复活其内部文件？
- [ ] 复活目录时，是否递归用了 `!xxx/**`？
- [ ] 用 `git rm --cached` 前，是否已通知团队？
- [ ] 批量刷新前，是否已确认 `.gitignore` 万无一失且 `git status` 干净？
- [ ] 全局忽略是否已经配置，避免个人系统垃圾混入仓库？


