在 Markdown 中跳转到其他文件夹的文件，核心语法与跳转当前目录文件一致，都是使用 `[链接文本](路径)` 格式，区别仅在于**路径的写法**。

以下是几种常见的跨文件夹跳转方式：

### 1. 相对路径（最推荐）

相对于当前 `.md` 文件的位置进行跳转，移植性最好。

|场景|语法示例|说明|
|:--|:--|:--|
|子文件夹中的文件|`[文档](./docs/guide.md)`|`./` 表示当前目录，可省略|
|上级文件夹中的文件|`[返回](../README.md)`|`../` 表示上一级目录|
|跨多级文件夹|`[配置](../../config/settings.md)`|连续使用 `../` 向上回溯|
|同级其他文件夹|`[关于](../about/intro.md)`|先上溯再进入新文件夹|

### 2. 绝对路径

从项目/仓库根目录开始定位（注意：**不是**操作系统的磁盘根目录）。

```markdown
[首页](/docs/index.md)       ← 从项目根目录开始
[API文档](/src/api/readme.md)
```

> ⚠️ **兼容性提醒**：以 `/` 开头的绝对路径在 GitHub/GitLab 等平台有效，但在本地编辑器（如 VS Code、Obsidian）中可能无法正确预览跳转，建议优先使用相对路径。

### 3. 跳转到目标文件的指定标题/锚点

```markdown
[安装步骤](./docs/setup.md#环境配置)
```

在路径后加 `#` + 标题ID（标题转小写、空格变 `-`、去除特殊字符）。

### ⚠️ 常见注意事项

- **中文文件名/路径**：部分平台需要 URL 编码，但 GitHub、GitLab、VS Code 等主流工具已原生支持中文路径，通常无需手动编码。
- **路径分隔符**：始终使用正斜杠 `/`，不要使用 Windows 的反斜杠 `\`。
- **大小写敏感**：Linux/macOS 系统及 Git 仓库对大小写敏感，`Docs/guide.md` 和 `docs/guide.md` 是不同路径。
- **验证链接**：写完后可在编辑器中按住 `Ctrl/Cmd + Click` 测试链接是否能正确跳转。

### 快速示例

假设你的项目结构如下：

```
project/
├── README.md          ← 当前文件
├── docs/
│   ├── guide.md
│   └── api/
│       └── reference.md
└── src/
    └── config.md
```

在 `README.md` 中：

```markdown
- [快速指南](./docs/guide.md)           ✅ 子文件夹
- [API参考](./docs/api/reference.md)    ✅ 嵌套子文件夹
- [配置文件](./src/config.md)           ✅ 同级其他文件夹
- [指南-安装章节](./docs/guide.md#安装)  ✅ 带锚点
```

掌握相对路径的 `./` 和 `../` 就能应对绝大多数跨文件夹跳转需求。